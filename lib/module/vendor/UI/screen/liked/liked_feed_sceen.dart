import 'package:flutter/material.dart';

class LikedFeedSceen extends StatelessWidget {
  const LikedFeedSceen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Column(children: [Text("Liked Feeds")]));
  }
}
